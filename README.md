### Mikoto Demo

```
virtualvenv venv
source venv/bin/activate
pip install -r pip-req.txt
git submodule init
git submodule sync
git submodule update
python app.py
go http://localhost:5000/
```

random change
keep change
